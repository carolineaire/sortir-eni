{% extends 'base.html.twig' %}

{% block title %}Sortie.com{% endblock %}

{% block body %}

<div class="container-fluid d-flex justify-content-end">
    <div class="informations text-end">
        <p>Date du jour : <span id="date-du-jour"></span></p>
        {% if app.user %}
            <p>
                Participant :
                <span>{{ app.user.prenom }} {{ app.user.nom|slice(0,1) }}.</span>
            </p>
        {% endif %}
    </div>
</div>

<div class="filtres mt-4">
    <!-- Bloc filtre -->
    <div class="p-4">
        <h4 class="mb-4">Filtrer les sorties</h4>
        <div class="row">
            <!-- Colonne gauche -->
            <div class="col-md-6">
                <!-- Site -->
                <div class="mb-3">
                    <label for="site" class="form-label">Site :</label>
                    <select id="site" class="form-select">
                        <option selected>SAINT HERBLAIN</option>
                        <option>NANTES</option>
                        <option>RENNES</option>
                    </select>
                </div>
                <!-- Nom de la sortie -->
                <div class="mb-3">
                    <label for="search" class="form-label">
                        Le nom de la sortie contient :
                    </label>
                    <div class="input-group">
                        <span class="input-group-text">🔍</span>
                        <input type="text" id="search" class="form-control" placeholder="search">
                    </div>
                </div>
                <!-- Dates -->
                <div class="mb-3">
                    <label class="form-label d-block">Entre :</label>
                    <div class="d-flex align-items-center gap-2">
                        <input type="date" class="form-control">
                        <span>et</span>
                        <input type="date" class="form-control">
                    </div>
                </div>
            </div>
            <!-- Colonne droite -->
            <div class="col-md-6 d-flex flex-column justify-content-center">
                <div class="form-check mb-2">
                    <input class="form-check-input" type="checkbox" checked id="orga">
                    <label class="form-check-label" for="orga">
                        Sorties dont je suis l'organisateur/trice
                    </label>
                </div>
                <div class="form-check mb-2">
                    <input class="form-check-input" type="checkbox" checked id="inscrit">
                    <label class="form-check-label" for="inscrit">
                        Sorties auxquelles je suis inscrit/e
                    </label>
                </div>
                <div class="form-check mb-2">
                    <input class="form-check-input" type="checkbox" checked id="nonInscrit">
                    <label class="form-check-label" for="nonInscrit">
                        Sorties auxquelles je ne suis pas inscrit/e
                    </label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="passees">
                    <label class="form-check-label" for="passees">
                        Sorties passées
                    </label>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid mt-4">
    <table class="table-sm table-bordered">
        <thead>
        <tr>
            <th scope="col">Titre</th>
            <th scope="col">Durée (heures)</th>
            <th scope="col">Date de début</th>
            <th scope="col">Fin des inscription</th>
            <th scope="col">Places restantes</th>
            <th scope="col">Lieu</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for s in sortie %}
            <tr>
                <td>{{ s.nom }}</td>
                <td>{{ s.duree }}</td>
                <td>{{ s.dateDebut | date('d/m/Y') }}</td>
                <td>{{ s.dateCloture | date('d/m/Y') }}</td>
                <td>{{ s.nbInscriptionMax }}</td>
                <td>{{ s.noLieux.nomLieu }}</td>
                <td>
                    <a href="#" tooltip="voir le détail de la sortie">
                        Détails
                    </a>
                    <a href="#" tooltip="s'inscrire à la sortie">
                        S'inscrire
                    </a>
                     <a href="#" tooltip="annuler la sortie">
                        Annuler
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucune donnée dans la base</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>

<script>
  document.getElementById('date-du-jour')
    .textContent = new Date().toLocaleDateString('fr-FR');
</script>
{% endblock %}

